This folder serves static assets for the frontend. To change the app icon:

1) Replace `favicon.png` with your own square image (PNG recommended).  
2) Hard refresh the browser (Cmd/Ctrl + Shift + R) to see the update.

Tip: Use a simple, high-contrast image so it stays recognizable at 16px.
